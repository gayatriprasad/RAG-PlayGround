[project]
name = "raglab"
version = "0.1.0"
description = "RAG learning laboratory"
requires-python = ">=3.10"
dependencies = [
  "pyyaml",
  "pydantic>=2",
  "tqdm",
  "psutil",
  "numpy",
  "pypdf",
  "pdfplumber",
  "python-docx",
  "huggingface-hub",
  "datasets",
  "tiktoken",
  "spacy",
  "openai",
  "chromadb",
  "sentence-transformers",
  "rank-bm25",
  "flashrank",
  "transformers",
  "sentencepiece",
  "torch",
  "pandas",
  "tabulate",
  "typer",
  "fastapi",
  "uvicorn[standard]",
  "httpx",
  "diskcache",
  # "pageindex",  # SKILL 06: May require manual installation from VectifyAI
  "networkx",  # SKILL 17: GraphRAG
]

[project.optional-dependencies]
# SKILL 15: Multi-agent architecture (optional)
agents = [
  "langgraph",
  "langchain-core"
]

# SKILL 18: Production observability (optional)
observability = [
  "mcp>=0.9.0",
  "langfuse>=2.0.0"
]

# All optional features
all = [
  "langgraph",
  "langchain-core",
  "mcp>=0.9.0",
  "langfuse>=2.0.0"
]

[project.scripts]
raglab-run = "raglab.run_experiment:app"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
